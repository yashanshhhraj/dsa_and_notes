#include <iostream>
using namespace std;

int main()
{
    // ! Unlike compile-time errors, runtime errors can only be detected while the program is running.
    // ? Taking user input --> 'cin'
    // int a, b;
    // cout << "Enter First Number : ";
    // cin >> a;
    // cout << "Enter Second Number : ";
    // cin >> b;
    // cout << "Enter First Number : ", cin >> a;
    // cout << "Enter Second Number : ", cin >> b;
    // cout << "The sum is: ";
    // cout << a + b << endl;
    // cout << "The sum is: " << a + b << endl;

    // int num = 10;
    // char chrctr = 'i';
    // num = chrctr;
    // cout << num << endl;
    // cout << chrctr << endl;

    // ! Unary Operators
    // ? 1. Increment Operator
    // int numb = 5;
    // numb++; // The value of the variable is incremented first and then It is used in the program
    // ++numb; // The value of the variable is assigned first and then It is incremented.
    // cout << numb << endl;

    // TODO 2. Decrement Operator

    // Logical Not Operator :-
    // int log = 0000;
    // int log2 = 4;
    // cout << !log << endl;
    // cout << !log2 << endl;

    // int a = 6, b = 4;

    // // Assignment Operator
    // cout << "a = " << a << endl;

    // //  Add and Assignment Operator
    // cout << "a += b is " << (a += b) << endl;

    // // Subtract and Assignment Operator
    // cout << "a -= b is " << (a -= b) << endl;

    // //  Multiply and Assignment Operator
    // cout << "a *= b is " << (a *= b) << endl;

    // //  Divide and Assignment Operator
    // cout << "a /= b is " << (a /= b) << endl;
}