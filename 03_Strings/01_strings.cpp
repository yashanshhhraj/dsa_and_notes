#include<iostream>
#include<vector>
using namespace std;

int main()
{
    // ? Fixed length array
    // char arr[6] = {'a', 'p', 'p', 'l', 'e'};
    // cout << arr << endl;

    // ? Variable length array
    // char arr[20];
    // cin >> arr;
    // cout << arr << endl;

    // string s = "Yash";
    // cout << s << endl;

    // ? Getting size of string
    // cout << s.size();

    // 'getline()' Method
    // string s;
    // getline(cin, s);
    // cout << s ;

    // ? Concatenation of string
    // string s1 = "Raman", s2 = " Chaman";
    // string s3 = s1 + s2; // ? Method 1
    // string s3 = s1.append(s2); // ? Method 2
    // cout << s3;

    // string s = "String";
    // ? Adding element at last of the string
    // s.push_back('s'); // ? Method 1
    // s = s + 'p'; // ? Method 2

    // ? Removing element from last of the string
    // s.pop_back();
    // cout << s;

    // ?  Escape Character
    // string message = "Hello, this is \"Yashansh\"";
    // cout << message;
    // string s = "\\0"; // ? \0
    // cout << s << endl;

    // ? Calculating size of string without 'size()' method
    // string s = "Raman";
    // int size = 0;

    // while(s[size] != '\0')
    // size++;

    // cout << size;

    // ? Cecking if the string is palindrome or not
    // string s = "geeksforgeeks";
    // int low = 0, high = s.size() - 1;

    // while(low < high)
    // {
    //     if(s[low] != s[high])
    //     {
    //         cout << "String is not palindrome" << endl;
    //         return 0;
    //     }

    //     low++,high--;
    // }
    
    // cout << "String is palindrome" << endl;

    // vector<string> arr(9);
}